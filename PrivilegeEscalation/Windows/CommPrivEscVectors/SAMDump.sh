impacket-secretsdump -sam SAM -system SYSTEM LOCAL


# This files are stored frecuently on C:\Windows\System32 directory but usually we can't reach them because are frecuently in use. We can dump its contents with mimikatz instead. However, if we find a windows.old directory we can attemp to download this files and his contents.
